#include "main.h"
/**
 * _abs - computes the absolute value of a number.
 * @n: number whose absolute value will be shown
 * Return: 0. (Success)
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
